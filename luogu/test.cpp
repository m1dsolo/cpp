#include <iostream>
#include <algorithm>

using namespace std;

int main() {
    vector<int> v {1, 5, 6, 4, 2, 3}, order(v.size());
    iota(order.begin(), order.end(), )
    sort(v.begin(), v.end())

    return 0;
}

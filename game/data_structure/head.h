#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <stddef.h>
#include <string.h>

typedef struct list List;
typedef struct list_iterator ListIterator;
typedef struct stack Stack;
typedef struct vector Vector;
typedef struct pair Pair;
typedef struct hash_table HashTable;
typedef struct unordered_map UnorderedMap;
typedef struct unordered_map_iterator UnorderedMapIterator;

typedef enum hash_table_key_t HASH_TABLE_KEY_T;
typedef enum unordered_map_key_t UNORDERED_MAP_KEY_T;


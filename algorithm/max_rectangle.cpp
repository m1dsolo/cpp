#include <iostream>
#include <vector>

using namespace std;

int get_max_matrix_area(vector<vector<bool>> &matrix) {
    int row, matrix.size(), col = matrix[0].size();
    for (int i = 0;)
} 
int main() {


    return 0;
}
